export type Childern = Readonly<{children : React.ReactNode}>

export type MenuItem = {
    name : string
    url : string
}

export type Children = {
    children : Readonly<React.ReactNode>
}

export const Genders = ['Male', 'Female']